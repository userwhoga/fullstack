# 🔧 Настройка H2 Console

## ❌ Проблема

При попытке подключиться к H2 Console вы видите ошибку:
```
Database "C:/Users/User/test" not found
```

Это происходит потому, что в форме подключения указан неправильный JDBC URL.

---

## ✅ Правильные настройки для подключения

### Шаг 1: Откройте H2 Console

Перейдите по адресу: **http://localhost:8081/h2-console**

### Шаг 2: Заполните форму подключения

Используйте **ТОЧНО** эти настройки:

| Поле | Значение |
|------|----------|
| **Saved Settings** | `Generic H2 (Embedded)` |
| **Setting Name** | `Generic H2 (Embedded)` |
| **Driver Class** | `org.h2.Driver` |
| **JDBC URL** | `jdbc:h2:mem:cardb` ⚠️ **ВАЖНО!** |
| **User Name** | `sa` |
| **Password** | *(оставьте пустым)* |

### Шаг 3: Нажмите "Connect"

После подключения вы увидите интерфейс H2 Console с таблицами.

---

## 📝 Важные моменты

### ⚠️ JDBC URL должен быть: `jdbc:h2:mem:cardb`

**НЕ используйте:**
- ❌ `jdbc:h2:~/test` (это создает файловую базу)
- ❌ `jdbc:h2:file:~/test` (это тоже файловая база)

**Используйте:**
- ✅ `jdbc:h2:mem:cardb` (in-memory база данных)

### Почему `mem:cardb`?

- `mem:` означает, что база данных находится в памяти (in-memory)
- `cardb` - это имя базы данных, указанное в `application.properties`

---

## 🔍 Проверка подключения

После успешного подключения вы сможете:

1. **Просмотреть таблицы:**
   ```sql
   SHOW TABLES;
   ```

2. **Просмотреть данные из таблицы CAR:**
   ```sql
   SELECT * FROM CAR;
   ```

3. **Просмотреть данные из таблицы OWNER:**
   ```sql
   SELECT * FROM OWNER;
   ```

4. **Просмотреть пользователей:**
   ```sql
   SELECT * FROM APP_USER;
   ```

---

## 🆘 Если не подключается

### Проблема 1: "Database not found"

**Причина:** Неправильный JDBC URL

**Решение:** Убедитесь, что используете `jdbc:h2:mem:cardb`

### Проблема 2: "Connection refused"

**Причина:** Бэкенд не запущен

**Решение:**
1. Убедитесь, что Spring Boot приложение запущено
2. Проверьте, что оно работает на порту 8081
3. Откройте http://localhost:8081/swagger-ui.html для проверки

### Проблема 3: "Access denied"

**Причина:** Неправильный username или password

**Решение:**
- Username: `sa`
- Password: *(пустое поле, не вводите ничего)*

---

## 📸 Скриншот правильных настроек

```
┌─────────────────────────────────────────┐
│ Saved Settings: Generic H2 (Embedded)   │
│                                         │
│ Setting Name: Generic H2 (Embedded)    │
│                                         │
│ Driver Class: org.h2.Driver            │
│                                         │
│ JDBC URL: jdbc:h2:mem:cardb            │ ← ВАЖНО!
│                                         │
│ User Name: sa                           │
│                                         │
│ Password: [пусто]                       │
│                                         │
│         [Connect]  [Test Connection]   │
└─────────────────────────────────────────┘
```

---

## 💡 Дополнительная информация

### In-Memory vs File Database

**In-Memory (`jdbc:h2:mem:cardb`):**
- ✅ База данных создается в памяти при запуске приложения
- ✅ Быстрая работа
- ❌ Данные теряются при остановке приложения
- ✅ Идеально для разработки и тестирования

**File Database (`jdbc:h2:file:~/test`):**
- ✅ Данные сохраняются на диск
- ✅ Данные не теряются при перезапуске
- ❌ Медленнее in-memory
- ✅ Подходит для production

В вашем проекте используется **in-memory** база данных, поэтому используйте `jdbc:h2:mem:cardb`.

---

## ✅ Готово!

После правильной настройки вы сможете:
- ✅ Подключаться к H2 Console
- ✅ Просматривать таблицы и данные
- ✅ Выполнять SQL запросы
- ✅ Отлаживать базу данных

**Удачи! 🚀**








