# 🚀 Инструкция по запуску Full-Stack проекта

## 📋 Содержание
1. [Требования](#требования)
2. [Запуск Бэкенда (Spring Boot)](#запуск-бэкенда)
3. [Запуск Фронтенда (React + Vite)](#запуск-фронтенда)
4. [Тестирование приложения](#тестирование)
5. [Решение проблем](#решение-проблем)

---

## 🔧 Требования

### Для Бэкенда:
- **Java 17** или выше
- **Gradle** (встроен в проект - gradlew)

### Для Фронтенда:
- **Node.js 16+** (рекомендуется 18 или выше)
- **npm** или **yarn**

---

## 🟢 Запуск Бэкенда

### Шаг 1: Перейдите в директорию бэкенда
```bash
cd "C:\Users\User\Downloads\Full_Stack\Full_Stack"
```

### Шаг 2: Запустите Spring Boot приложение

**Windows:**
```bash
.\gradlew.bat bootRun
```

**Linux/Mac:**
```bash
./gradlew bootRun
```

### ✅ Проверка запуска бэкенда

Бэкенд должен запуститься на порту **8081**. Откройте браузер и проверьте:

- **API Documentation (Swagger):** http://localhost:8081/swagger-ui.html
- **H2 Database Console:** http://localhost:8081/h2-console
  - JDBC URL: `jdbc:h2:mem:cardb`
  - Username: `sa`
  - Password: *(оставьте пустым)*

### 📝 Тестовые учетные записи:
- **User:** username: `user`, password: `user`
- **Admin:** username: `admin`, password: `admin`

---

## 🔵 Запуск Фронтенда

### Шаг 1: Перейдите в директорию фронтенда
```bash
cd "C:\Users\User\Downloads\untitled4\untitled4"
```

### Шаг 2: Установите зависимости (только первый раз)
```bash
npm install
```

### Шаг 3: Запустите dev-сервер
```bash
npm run dev
```

### ✅ Проверка запуска фронтенда

Фронтенд должен запуститься на порту **5173**. Откройте браузер:

- **Приложение:** http://localhost:5173

---

## 🧪 Тестирование приложения

### 1. Войдите в систему
- Откройте http://localhost:5173
- Используйте учетные данные: `user/user` или `admin/admin`

### 2. Проверьте функциональность
- ✅ **READ** - Просмотр списка машин
- ✅ **CREATE** - Добавление новой машины (кнопка "+ Create New Car")
- ✅ **UPDATE** - Редактирование машины (иконка карандаша)
- ✅ **DELETE** - Удаление машины (иконка корзины)
- ✅ **Export CSV** - Экспорт данных в CSV файл
- ✅ **Logout** - Выход из системы

---

## 🔧 Решение проблем

### Проблема: "Failed to load cars"

**Причина:** Бэкенд не запущен или недоступен.

**Решение:**
1. Убедитесь, что бэкенд запущен на порту 8081
2. Проверьте логи бэкенда на наличие ошибок
3. Откройте http://localhost:8081/swagger-ui.html для проверки API

### Проблема: CORS ошибки в консоли браузера

**Причина:** Неправильная настройка CORS.

**Решение:**
1. Убедитесь, что файл `SecurityConfig.java` обновлен (разрешены origins для localhost:5173)
2. Перезапустите бэкенд

### Проблема: 401 Unauthorized после входа

**Причина:** JWT токен не сохраняется или не отправляется.

**Решение:**
1. Откройте DevTools браузера (F12)
2. Перейдите во вкладку "Network"
3. Проверьте запрос `/login` - должен вернуть заголовок `Authorization` с токеном
4. Проверьте LocalStorage - должен быть сохранен `jwt` токен

### Проблема: Порты заняты

**Для бэкенда (8081):**
```bash
# Windows
netstat -ano | findstr :8081
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :8081
kill -9 <PID>
```

**Для фронтенда (5173):**
```bash
# Windows
netstat -ano | findstr :5173
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :5173
kill -9 <PID>
```

---

## 📂 Структура проекта

### Бэкенд (C:\Users\User\Downloads\Full_Stack\Full_Stack)
```
Full_Stack/
├── src/main/java/com/packt/cardatabase/
│   ├── CardatabaseApplication.java     # Главный класс + инициализация БД
│   ├── config/
│   │   ├── SecurityConfig.java         # Настройки безопасности + CORS
│   │   ├── AuthenticationFilter.java   # JWT фильтр
│   │   └── AuthEntryPoint.java        # Обработка ошибок аутентификации
│   ├── controller/
│   │   ├── LoginController.java        # Эндпоинт /login
│   │   └── CarRestController.java      # CRUD API /api/cars
│   ├── domain/                         # Entity классы
│   ├── dto/                            # DTO классы
│   └── service/                        # Сервисы (JWT, UserDetails)
└── src/main/resources/
    └── application.properties          # Конфигурация (порт 8081, H2 БД)
```

### Фронтенд (C:\Users\User\Downloads\untitled4\untitled4)
```
untitled4/
├── src/
│   ├── App.tsx                        # Главный компонент
│   ├── api/
│   │   └── carService.ts              # API клиент (axios)
│   ├── components/
│   │   ├── Login.tsx                  # Форма входа
│   │   ├── CarList.tsx                # Список машин + CRUD
│   │   ├── AddCar.tsx                 # Добавление машины
│   │   └── EditCar.tsx                # Редактирование машины
│   └── types/
│       └── Car.ts                     # TypeScript типы
├── vite.config.ts                     # Конфигурация Vite + proxy
└── package.json                       # Зависимости npm
```

---

## 🎯 Что было исправлено

### В бэкенде:
1. ✅ **SecurityConfig.java** - исправлена CORS конфигурация:
   - Изменено `allowedOrigins` с `*` на конкретные порты (5173, 3000)
   - Добавлен `setExposedHeaders` для экспонирования заголовка `Authorization`
   - Установлен `setAllowCredentials(true)` для работы с куками/токенами

### Во фронтенде:
2. ✅ **vite.config.ts** - добавлен proxy для API запросов:
   - `/api` -> `http://localhost:8081`
   - `/login` -> `http://localhost:8081`

---

## 🌐 API Endpoints

### Authentication
- **POST** `/login` - Вход в систему (возвращает JWT токен)

### Cars CRUD
- **GET** `/api/cars` - Получить все машины
- **GET** `/api/cars/{id}` - Получить машину по ID
- **POST** `/api/cars` - Создать новую машину
- **PUT** `/api/cars/{id}` - Обновить машину
- **DELETE** `/api/cars/{id}` - Удалить машину

---

## 📚 Дополнительно

### Swagger UI
Swagger UI доступен по адресу: http://localhost:8081/swagger-ui.html

Здесь вы можете:
- Просмотреть все доступные API endpoints
- Протестировать API напрямую из браузера
- Посмотреть схемы запросов/ответов

### H2 Console
H2 Console доступен по адресу: http://localhost:8081/h2-console

Используйте для:
- Просмотра структуры БД
- Выполнения SQL запросов
- Отладки данных

---

## 🎉 Готово!

Теперь ваш Full-Stack проект должен работать корректно. Если возникнут проблемы, проверьте раздел "Решение проблем" выше.

**Happy Coding! 🚀**








